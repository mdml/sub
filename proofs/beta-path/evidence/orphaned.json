{
  "precondition": "The implementation-private state identified a live `$SUB_BIN __supervise $HANDLE 1` process. Its PID was verified and then omitted from this evidence.",
  "command": "sub inspect $HANDLE --state-dir $STATE_DIR",
  "task": { "handle": { "id": "$HANDLE" }, "harness": "codex", "status": "orphaned" },
  "attempts": [
    { "number": 1, "status": "orphaned", "harness_session_id": "$SESSION_ID" }
  ]
}
